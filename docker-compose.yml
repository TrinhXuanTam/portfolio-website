version: '3.7'

services:
  portfolio-website:
    env_file:
      - .env
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - $PORT:$PORT
    volumes:
      - ./src:/portfolio-website/src
    command: npm run dev
